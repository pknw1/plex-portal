#!/bin/bash
PASSWORD=password

groupadd -g 666 plex

for users in plex docker;
do
	sudo useradd -b /home -m -g 666 -o -u 666 -s /bin/bash -p $PASSWORD $users
	sudo echo   $users' ALL=(ALL) NOPASSWD:ALL'  > /etc/sudoers.d/"${users}"
done

exit 